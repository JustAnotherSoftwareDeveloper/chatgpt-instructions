# Archived Shopping Implementation

This directory preserves the previous Shopping implementation for migration/reference while the live project is rebuilt.

These files are source-control-only migration material:
- they are not live Shopping authorities;
- do not route to them or apply them implicitly;
- do not include `archive/` in the deployed/uploaded ChatGPT Project file set;
- migrate useful behavior deliberately into the correct live authority or workflow.

Except for this README, preserve archived historical files unchanged.
