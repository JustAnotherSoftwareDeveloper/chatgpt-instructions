# Archived Shopping Implementation

This directory contains the previous Shopping implementation preserved for migration/reference while the live project root is rebuilt around inheritance-based product context.

These files are not live authorities. Do not route to them or apply them implicitly.
